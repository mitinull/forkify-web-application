{
  "name": "forkify",
  "version": "1.0.0",
  "description": "Ingredient and recipe app",
  "default": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html",
    "build": "parcel build index.html --no-scope-hoist"
  },
  "author": "mohammad",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "core-js": "^3.26.0",
    "fractional": "^1.0.0",
    "regenerator-runtime": "^0.13.10"
  }
}
